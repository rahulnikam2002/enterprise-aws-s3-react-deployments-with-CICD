name: PR Validation

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read
  pull-requests: read

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
    secrets: inherit